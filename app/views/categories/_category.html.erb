<li id="category_<%= category.id %>"> <!-- ID ที่ไม่ซ้ำกันสำหรับแต่ละ Category -->
  <%= category.name %>
  <%= link_to 'Edit', edit_category_path(category) %>
  <%= link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' } %>
</li>
